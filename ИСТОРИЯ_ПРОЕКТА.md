# История проекта КлавирON

## 04.07.2026 — Первый аудит
- `klaviron-saas-2026-07-04.md` в папке `landing-audits`
- Выявлены критические проблемы: Tailwind CDN, verifyPayment заглушка, GAS API не работает

## 06.07.2026 — Повторный аудит + фиксы
- `klaviron-saas-2026-07-06.md` — сравнение с прошлым аудитом
- `klaviron-saas-2026-07-07-fixes.md` — отчёт об исправлениях
- **Коммит `fd971cd`:** experience scoring + is-selected highlighting + fallback toast + cache no-store
  - Fix #2: `experience` теперь влияет на warnings
  - Fix #3: Подсветка выбранной опции при возврате «Назад»
  - Fix #4: console.error + toast при fallback на локальный каталог
  - Fix #5: `cache: 'no-store'` для GAS API

## 07.07.2026 — Текстовые правки и реорганизация

### Коммит `3ab25e0` — Тексты лендинга
- «Продакшен» → «Для звукозаписи» (категории)
- «DAW» → «цифровая студия» (везде)
- «нотная полка» → «подставка для нот (пюпитр)» (везде)
- «Первый инструмент» — обновлено описание
- «Количество клавиш» — 61 = компактность и мобильность
- «Тип клавиатуры» — синтезаторная (органная) / молоточковая (рояльная)
- «Встроенные динамики» — большие сцены, студии
- «Аккомпанемент» — «оркестр под руками»
- Квиз: формат, звуки, динамики, аккомпанемент — новые формулировки
- Yamaha CK61 → Yamaha CP88 (stage.high)

### Коммит `457cdce` — Merge-логика
- Если API не возвращает модели → использовать localData целиком
- Исправляет несоответствие типа и моделей

### Коммит `d8c22f2` — Новые бюджеты + цены
- Диапазоны бюджета: До 20 000 / 20 000–50 000 / 50 000–90 000 / От 90 000
- Цены актуализированы по Google Sheets (32 модели)
- realPrice диапазоны обновлены (18 записей)

### Коммит `d77e11a` — «продакшен» → «звукозапись»
- Кнопка цели в квизе: «Создание музыки / звукозапись»

### Коммит `dbd3942` — Финальная очистка
- «Универсальный синтезатор / старший аранжировщик» → «Синтезатор-аранжировщик высокого класса»
- «Нотная полка» → «Подставка для нот (пюпитр)» — окончательно (0 остатков)

### Коммит `b2b8e5e` — Формулировка why
- «Это верх любительского / полупро сегмента» → «На стыке полупрофессионального и профессионального сегментов»

### Коммит `94dd43a` — Формулировка why (production.high)
- «Вы можете хотеть и встроенные звуки, и работу без компьютера» → «Вы можете использовать встроенные звуки, и подключение к компьютеру»

## 07.07.2026 — Новый адрес

### Организация `klaviron` на GitHub
- Создана организация: `klaviron`
- Создан репозиторий: `klaviron.github.io`
- Код запушен в организацию
- **Новый адрес:** https://klaviron.github.io — без фамилии!
- **Старый адрес:** https://zakharovvb.github.io/klaviron-saas/ — работает как backup

## 07.07.2026 — Исследование платёжных систем

### ЮKassa
- Регистрация начата на https://yookassa.ru
- Выбран способ: «Платежи на сайте»
- **Требования к сайту (нужно доделать):**
  1. Описание услуги + фиксированная цена (например, 299 ₽)
  2. Способ получения (цифровой товар — отображается на сайте)
  3. Пользовательское соглашение / оферта (terms.html — дополнить)
  4. Контакты и реквизиты: ФИО, ИНН самозанятого, e-mail
- **Комиссия:** 2.9%
- **Минусы:** длительная модерация, высокие требования к сайту

### Альтернативы (исследованы):

| Сервис | Комиссия | Модерация | Требования к сайту | Счёт |
|--------|----------|-----------|-------------------|------|
| **Tinkoff Kassa** | 1.6% | 1 день | Средние | Желательно Т-Банк |
| **Prodamus** | 2.0% | 1-2 дня | Минимальные | Любой банк |
| **CloudPayments** | 2.5% | Средняя | Средние | Любой банк |
| **Robokassa** | 2.9% | Средняя | Средние | Любой банк |
| **ЮKassa** | 2.9% | Длинная | Высокие | Любой банк |

### Рекомендация:
- **Tinkoff Kassa** — если открыть счёт в Т-Банке (1.6%, быстро)
- **Prodamus** — если использовать текущий счёт в любом банке (2.0%, лояльнее)

### Что нужно сделать перед подключением любой платёжной системы:
1. Добавить на сайт блок с описанием платной услуги + цена
2. Обновить terms.html (полная оферта)
3. Добавить контакты: ФИО, ИНН, e-mail
4. Указать способ получения (цифровой товар)

## 07.07.2026 — Интеграция Tinkoff Kassa (в процессе)

### Решение
- **Провайдер:** Tinkoff Kassa (Т-Банк)
- **Причина выбора:** комиссия 1.6%, быстрая модерация (~1 день), есть счёт в Т-Банке
- **Статус самозанятости:** достаточно! ООО НЕ нужно — Tinkoff Kassa работает с самозанятыми (НПД)

### Реквизиты (получены от владельца)
- **ФИО:** Захаров Василий Борисович
- **ИНН:** 272322019546
- **Регион:** Хабаровский край
- **E-mail:** zakharov0073@yandex.ru
- **Цена услуги:** 299 ₽
- **Счёт в Т-Банке:** есть

### Коммит `b17e037` — Подготовка к Tinkoff Kassa
- `terms.html` — обновлена оферта: ФИО, ИНН, цена 299₽, Tinkoff Kassa, НПД, e-mail
- `index.html` — добавлен футер с реквизитами и ссылками на terms/privacy
- `tinkoff_backend.gs` — готовый бэкенд: Init API, webhook, verify, orders
- `PLAN_TINKOFF.md` — пошаговый план интеграции (9 шагов)
- `REKVIZITY.md` — шаблон реквизитов (заполнен)
- Запушено в `origin/main` и `org/main`

### Статус по шагам
| Шаг | Статус | Описание |
|-----|--------|----------|
| 0 | ✅ | План интеграции (PLAN_TINKOFF.md) |
| 1 | ✅ | Данные получены |
| 2 | ✅ | terms.html — оферта с реквизитами |
| 3 | ✅ | Футер с контактами на лендинге |
| 4 | ✅ | Блок «299₽» на лендинге — в процессе (прерван) |
| 5 | ✅ | Paywall в квизе (DEV-режим) |
| 6 | ✅ | tinkoff_backend.gs — бэкенд готов |
| 7 | ⏳ | Регистрация в Tinkoff Kassa — авторизация завтра (08.07.2026) |
| 8 | ❌ | Тестирование оплаты — после ключей |
| 9 | ❌ | Деплой в продакшен |

### Важно: ООО не нужно!
Tinkoff Kassa принимает самозанятых (плательщиков НПД) без оформления ООО/ИП.
Достаточно: ИНН + статус самозанятого в «Мой налог» + сайт по требованиям.

### Что делать завтра (08.07.2026)
1. Зайти на https://www.tinkoff.ru/kassa/
2. Авторизоваться по паспорту/Т-Банк ID
3. Указать: самозанятый, ИНН 272322019546
4. Указать сайт: https://klaviron.github.io
5. Получить Terminal Key + Secret Key
6. Настроить URLs:
   - Success: `https://klaviron.github.io/?payment=ok`
   - Fail: `https://klaviron.github.io/?payment=fail`
   - Webhook: `https://script.google.com/macros/s/AKfycbyfFLVPQQXOVvhrQaXHSufbKr37WVW4fFArIVwHP3Hp_zEQRSskuIELHIHAQDLss5H1/exec`

### После получения ключей
1. Вставить ключи в Script Properties (Apps Script)
2. Обновить Code.gs в Google Apps Script (из tinkoff_backend.gs)
3. Переразвернуть Web App
4. Вернуть paywall в index.html (Шаг 5)
5. Тестовая оплата

### Коммит `76b09c7` — Смена e-mail
- `zakharov0073@gmail.com` → `zakharov0073@yandex.ru` (index.html, terms.html, ИСТОРИЯ_ПРОЕКТА.md)

### Коммит `ba3234e` — UI/UX фиксы по аудиту
- **Toast:** `pointer-events: none` + время 2 сек (не блокирует клики)
- **Post-payment экран:** при `?payment=ok` — spinner → экран «✅ Оплата подтверждена!» → кнопка «Смотреть результат»
- **#quiz scroll:** `scrollIntoView` в `quiz-start` и `resetQuiz` (плавный скролл к квизу)
- **SEO мета-теги:** canonical, og:title/description/url/image, twitter:card, favicon (🎹)
- Не меняли: шаг 4 заголовок, текст спиннера, © 2025–2026 (по решению владельца)

## 08.07.2026 — Стратегия монетизации (план, отложено)

### Решение
- **Один домен** (klaviron.github.io → позже klaviron.ru), **одна кодовая база**
- **Две внутренние ветки монетизации**, переключение по URL/UTM, без пользовательского тумблера:
  - `?mode=paid` (по умолчанию) — платный результат 299 ₽
  - `?mode=market` (будущее) — бесплатный результат + партнёрские ссылки Яндекс.Маркета
- **Не делать два домена** — дробит SEO, аналитику, поддержку

### Почему платная версия сначала
1. Товар специфичный — покупатели идут в спец. магазины, не на маркетплейс
2. Яндекс.Маркет партнёрка требует модерацию (2-14 дней), живой ресурс, контент
3. Нет подтверждённого спроса на 299 ₽ — нужно проверить first
4. Партнёрка Яндекса ограничивает источники трафика (таргет/контекст на бренд)

### План запуска (по этапам)
| Этап | Что делаем | Зачем |
|------|-----------|-------|
| 1 | Довести платную версию 299 ₽ до конца | Проверить willingness to pay |
| 2 | Запустить трафик на один оффер | Не размывать аналитику |
| 3 | Собрать первые 10–30 оплат | Подтверждение спроса |
| 4 | Опубликовать 10+ материалов (сайт/Telegram) | Усилить доверие, подготовить модерацию |
| 5 | Податься в Яндекс.Маркет партнёрку | С живой площадкой |
| 6 | Добавить `?mode=market` как апселл/альтернативу | Вторая монетизация |

### Yandex.Market — конкретные ссылки под каждую модель
- Не одна общая ссылка, а партнёрская под каждую рекомендованную модель
- Кнопка «Открыть на Яндекс.Маркете» в результате квиза
- URL — партнёрская ссылка на товар или поисковую выдачу по модели

### Метрики для сравнения (когда будут обе ветки)
- **market:** EPC = клики × конверсия_в_покупку × комиссия
- **paid:** RPV = конверсия_в_оплату × 299 ₽
- Побеждает ветка с большей выручкой на 100 посетителей

### Безопасный стартовый сценарий
- 80% трафика → paid
- 20% трафика → market (тест)
- Через 2-3 недели сравнить: доход/100 посетителей, completion rate, CTR, жалобы


## 08.07.2026 — Покупка домена klaviron.ru

### Регистрация
- **Домен:** `klaviron.ru`
- **Регистратор:** Beget (BEGET-RU)
- **Дата регистрации:** 08.07.2026
- **Действует до:** 08.07.2027
- **Статус Whois:** «зарегистрирован, делегирован, не проверен»
- **DNS-серверы:** ns1.beget.com, ns2.beget.com, ns1.beget.pro, ns2.beget.pro

### DNS-записи (настроены на Beget)
- **A-записи (4 шт.):** 185.199.108.153, 185.199.109.153, 185.199.110.153, 185.199.111.153
- **CNAME:** www → zakharovvb.github.io
- Лишняя A-запись Beget (5.101.152.161) — удалена

### GitHub Pages
- Custom domain: `klaviron.ru` ✅
- Site live: http://klaviron.ru/
- HTTPS: ожидает активации (после делегирования DNS)
- Enforce HTTPS: будет включён после выпуска SSL

### Статус
- DNS делегирован ✅ (Whois подтверждает)
- SSL-сертификат: ожидается (15-30 мин после делегирования)
- После активации HTTPS: обновить canonical, og:url в index.html на `https://klaviron.ru/`


## 08.07.2026 — Багфиксы по аудиту B1-B17

### Коммит `ba3234e` — UI/UX фиксы
- **Toast:** `pointer-events: none` + время 2 сек (B15)
- **Post-payment экран:** при `?payment=ok` — spinner → экран «✅ Оплата подтверждена!» (B-аудит)
- **#quiz scroll:** `scrollIntoView` в `quiz-start` и `resetQuiz` (B8)
- **SEO мета-теги:** canonical, og:title/description/url/image, twitter:card, favicon (B-SEO)

### Коммит `21fc8ac` — B1, B2, B12
- **B1:** Тип в allinone+hammer — «Цифровое пианино / сценическое пианино» → «Цифровое пианино»
- **B2:** Тип в stage+speakers — «Универсальный клавишный старт для репетиций» → «Синтезатор для репетиций и небольших выступлений»
- **B12:** realPrice пересчитан (14 диапазонов) по фактическим ценам моделей

### Коммит `8e145a9` — B5
- **B5:** accompaniment теперь влияет на warnings (4 сценария: hobby+no, stage+yes, learning+yes, allinone+no)
- Шаг 6 больше не мёртвый — все 7 ответов квиза используются

### Не исправлено (по решению владельца):
- B3: format для production — MIDI не имеет молоточковой (оставлено)
- B4: needBuiltInSounds для не-production — оставлено (вопрос полезен для GAS)
- B6-B11: следствия B3-B5, частично решены
- B13-B17: минорные/не баги

## 08.07.2026 — Покупка домена klaviron.ru

### Регистрация
- **Домен:** `klaviron.ru`
- **Регистратор:** Beget (BEGET-RU)
- **Дата регистрации:** 08.07.2026
- **Действует до:** 08.07.2027
- **Статус Whois:** «зарегистрирован, делегирован, не проверен»
- **DNS-серверы:** ns1.beget.com, ns2.beget.com, ns1.beget.pro, ns2.beget.pro

### DNS-записи (настроены на Beget)
- **A-записи (4 шт.):** 185.199.108.153, 185.199.109.153, 185.199.110.153, 185.199.111.153
- **CNAME:** www → zakharovvb.github.io

### GitHub Pages
- Custom domain: `klaviron.ru` ✅
- HTTP: работает (http://klaviron.ru → 200)
- HTTPS: ожидает SSL-сертификат от GitHub

### E-mail
- `zakharov0073@gmail.com` → `zakharov0073@yandex.ru` (коммит `76b09c7`)


## 08.07.2026 — Stage 1: Адаптивные шаги + подготовка Stage 2

### Коммит 6e710a8 — Stage 1: адаптивные шаги
- ALL_STEPS — полный массив шагов
- getVisibleSteps(goal) — фильтрует шаги по цели
- production: 5 шагов, stage: 6, остальные: 7

### Найдена готовая реализация Stage 2-5 (папка вариант/)
- quiz-engine.js (902 строки) — движок по ТЗ v1.1
- quiz-engine.test.js — 27 автотестов
- Покрывает: B1-B20, 22 ситуации, computeRealPrice, adjustBudget

### План интеграции (новая сессия):
1. Скопировать quiz-engine.js в проект
2. Подключить script
3. Заменить pickLocalResult на QuizEngine.pickResult
4. Обновить renderResult
5. Прогнать тесты
6. Git commit + push

### Откат (rollback):
Если новая логика не подойдет: git revert или git reset --hard 6e710a8
Стабильная версия до Stage 2: коммит 6e710a8

## 08.07.2026 — Stage 2: Интеграция quiz-engine.js

### Коммит `8904ce5` — QuizEngine v1.0 интегрирован
- `quiz-engine.js` скопирован из `вариант/` в корень проекта
- `<script src="quiz-engine.js"></script>` подключён перед основным скриптом
- `getVisibleSteps()` → делегирует к `QuizEngine.getStepsForGoal(goal)`
- `pickLocalResult()` → адаптер к `QuizEngine.pickResult()` (старый `localCatalog` закомментирован)
- `renderResult()` обновлён для новых полей: `tradeoff`, `nextSteps`, `upgradePath`
- Добавлены HTML-блоки: `#result-tradeoff-wrap`, `#result-next-steps-wrap`, `#result-upgrade-wrap`
- `resetQuiz()` сбрасывает новые поля
- `submitQuiz()` передаёт новые поля из `localData` в `renderResult`
- Автотесты: 27/27 прошли успешно (`node вариант/quiz-engine.test.js`)

### Что починено (B1-B20)
- B1: Тип «Цифровое пианино / сценическое пианино» → корректный тип
- B2: Stage+speakers=yes+low → полноценный тип, без warning
- B3: format для production скрыт (4 шага вместо 7)
- B5: accompaniment активно влияет на тип
- B8: adjustBudgetByExperience сдвигает budget, тип пересчитывается
- B10: Каталог перестроен — 22 ситуации, нет «дыр»
- B12: realPrice рассчитывается автоматически
- B18: warning «может, цифровое пианино?» убран
- B20: pickResult принимает исходный goal пользователя

### Коммит `f6e68bf` — Fix: learning + accompaniment=yes логика
- **Баг:** `learning + synth + accompaniment=yes` → выдавало цифровое пианино (без ритмов)
- **Фикс:** 
  - `synth + accompaniment=yes` → обучающий синтезатор (есть ритмы/аккомпанемент)
  - `hammer + accompaniment=yes` → цифровое пианино + warning про аккомпанемент
- Файлы синхронизированы: `quiz-engine.js` + `вариант/quiz-engine.js`
- Автотесты: 29/29 (добавлены S6a, S6b)

### Тег `v1.0-stable` — точка отката
- Создан на коммите `f6e68bf`
- Откат: `git reset --hard v1.0-stable`
- Стабильная версия: QuizEngine v1.0 + accompaniment fix

### Коммит `5ee6374` — GAS-бэкенд: getPreviewResult_ + getPaidResult_ + getHtml_
- Добавлены недостающие функции в `tinkoff_backend.gs` (+333 строки):
  - `getPreviewResult_` — бесплатное превью: type, summary, why, warnings (без моделей)
  - `getPaidResult_` — платный результат: модели, accessories, realPrice
  - `getHtml_` — редирект на klaviron.ru
  - `readCatalogFromSheet_` — чтение Google Sheets, динамический поиск колонок по заголовкам
  - `filterModels_` — фильтрация по столбцу J «Автоаккомпанемент» (да/нет)
  - `adjustBudgetGAS_` — корректировка бюджета по опыту
  - `determineTypeGAS_` — определение типа (с логикой learning+synth+accompaniment)
  - `buildWarningsGAS_` — формирование warnings
  - `buildAccessoriesGAS_` — формирование accessories
  - `computeRealPriceGAS_` — расчёт realPrice
- Столбец J в Google Sheets переименован: «Ритмы» → «Автоаккомпанемент» (заполнен да/нет)
- GAS-бэкенд готов к деплою — все функции написаны
- Для запуска: скопировать код в Apps Script → переразвернуть Web App

### Что осталось для боевого API
1. Скопировать код из `tinkoff_backend.gs` в редактор Apps Script
2. Переразвернуть Web App (Execute as: Me, Access: Anyone)
3. Протестировать: `?action=previewResult&goal=learning&budget=low&format=hammer&accompaniment=yes`
4. Получить ключи Tinkoff → вставить в Script Properties
5. Включить `PAYMENT_ENABLED = true` в `index.html`

## 08.07.2026 — Аудит безопасности + XSS-фиксы

### Коммит `fc1aeb3` — Security: XSS-1-6 + PAY-3 fixed
Проведён аудит безопасности klaviron.ru. Исправлены критические уязвимости:

| ID | Уязвимость | Severity | Фикс |
|----|-----------|----------|------|
| XSS-1 | `renderList` через innerHTML | 🔴 Критично | `createElement` + `textContent` |
| XSS-2 | `renderModels` через innerHTML | 🔴 Критично | `createElement` + `textContent` |
| XSS-3 | `renderAccessories` через innerHTML | 🔴 Критично | `createElement` + `textContent` |
| XSS-4 | `showError` через innerHTML | 🔴 Критично | `createElement` + `textContent` |
| XSS-5 | `paymentUrl` без валидации (open redirect) | 🟡 Высокий | `isSafeUrl()` — только http/https |
| XSS-6 | payload без валидации (инъекция в API) | 🟡 Средний | `sanitizePayload()` — whitelist |
| PAY-3 | `pendingOrderId` не очищается после сброса | 🟡 Средний | `pendingOrderId = null` в `resetQuiz` |

### Что НЕ исправлено (отложено)
- CSP-1: нет Content-Security-Policy (нужен рефакторинг onclick → addEventListener)
- SRI-1: CDN-скрипты без integrity-хешей
- PRIV-1: нет cookie banner (GDPR, не критично для RU)
- CLICK-1: нет frame-ancestors (clickjacking)
- DEP-1: Tailwind через CDN вместо build

### Тесты: 29/29 прошли после фиксов

## 09.07.2026 — Корректировка фраз результатов квиза

### Коммит `91d1db7` — quiz-engine: корректировка фраз (28 ситуаций + предупреждения + бюджет)
- Обновлены тексты в `quiz-engine.js` + `вариант/quiz-engine.js` (64 строки изменено)
- **HOBBY:** «Встроенные динамики - отличный вариант для дома», «Автоаккомпанемент - больше возможностей и эмоций при первом знакомстве с инструментом», «баланс для начинающих», «Этот тип инструмента даёт», «Ещё не студийная рабочая станция, но уже полноценные функции»
- **LEARNING:** «задуматься о переходе в mid-сегмент», «риск возможной потери интереса», «немного дороже», «ограничения в практике игры и музицировании», «правильное ощущение от контакта с клавиатурой», «Полная клавиатура в 88 клавиш - полный диапазон», «Лучший выбор для долгой практики»
- **PRODUCTION:** «создании музыкальных композиций» (вместо «продакшене»)
- **STAGE:** «Подключение к внешнему звукоусилению здесь - норма», переписано описание premium, «сложных комплексных решений», «фортепианная механика», «пианистического репертуара», «Для полноценных возможностей исполнения серьезного репертуара»
- **ALL-IN-ONE:** «Через год — рассматривать средний сегмент», «игры, обучения и творчества», «понятным и универсальным», «ощущение фортепианной клавиатуры», «ощущением клавиатуры настоящего пианино»
- **Предупреждения:** «базового освоения и первых шагов в изучении музыки», «Для обучения игре на фортепиано», «Встроенный базовый набор тембров»
- **Бюджет:** «в звукозаписи или сценическом сегменте»
- Автотесты: 29/29 ✅
- Запушено на оба репозитория (origin + org)

## 09.07.2026 — Миграция Tinkoff Kassa → ЮKassa

### Причина
Tinkoff Kassa требует ИП/ООО — самозанятый без ИП не подходит.
ЮKassa работает с самозанятыми напрямую (НПД, без ИП).

### Коммит `58ad84f` — feat: миграция с Tinkoff Kassa на ЮKassa
- **`yookassa_backend.gs`** — создан (758 строк): полная замена бэкенда
  - Авторизация: HTTP Basic Auth (shopId:secretKey) вместо SHA-256 подписи
  - Создание платежа: POST /v3/payments (рубли "299.00" вместо копеек 29900)
  - Webhook: JSON `{ event, object }` вместо form-urlencoded
  - Проверка: GET /v3/payments/{id} вместо POST /v2/GetState
  - Статус успеха: `succeeded` вместо `CONFIRMED`
  - Idempotence-Key header для защиты от дублей
  - DEV-режим сохранён (без ключей — мок-платёж)
- **`index.html`** — 3 упоминания «Tinkoff Kassa» → «ЮKassa»
- **`terms.html`** — пункт 4.3: «ЮKassa (ООО «ЮKassa», НКО «Яндекс.Деньги»)»
- **`PLAN_YOOKASSA.md`** — создан: план интеграции, отличия от Tinkoff, шаги развёртывания
- **`CLINE_CONTEXT.md`** — обновлён: все упоминания Tinkoff заменены на ЮKassa
- **`tinkoff_backend.gs`** — оставлен как архив (не используется)
- Фронтенд-логика без изменений: `fetch(API_URL + '?action=createpayment...')`
- GAS endpoint URL без изменений
- Запушено на оба репозитория (origin + org)

### Script Properties
- Удалить: `TINKOFF_TERMINAL_KEY`, `TINKOFF_SECRET_KEY`
- Добавить: `YUKASSA_SHOP_ID`, `YUKASSA_SECRET_KEY`

### Следующие шаги (после получения ключей ЮKassa)
1. Регистрация на https://yookassa.ru (самозанятый, ИНН 272322019546)
2. Получить Shop ID + Secret Key
3. Скопировать `yookassa_backend.gs` в Apps Script
4. Script Properties: `YUKASSA_SHOP_ID` + `YUKASSA_SECRET_KEY`
5. Переразвернуть Web App
6. Настроить webhook: `payment.succeeded`, `payment.canceled`
7. `PAYMENT_ENABLED = true` в `index.html`
8. Тестовая оплата 299 ₽

## 09.07.2026 — Подключение ЮKassa: ключи + бэкенд через clasp

### Получены ключи ЮKassa
- **ShopID:** 1402830
- **Secret Key:** `live_A7y...` (получен, встроен в код)
- **LegalID:** LB0003407965
- **MerchantID (СБП):** MA0006566414
- **МСС:** 9999
- СБП подключён для ShopID 1402830
- НСПК: логотип СБП в подвале не нужен (нет других лого платёжных систем)

### Коммит `f6d05e5` — PAYMENT_ENABLED=true
- `PAYMENT_ENABLED = true` в `index.html` — paywall активен
- API ЮKassa протестирован через curl: платёж 299 ₽ создаётся успешно
- Запушено на оба репозитория

### Развёртывание бэкенда через clasp (v3.3.0)
- Проверены все 4 существующих проекта Apps Script — ни один не бэкенд КлавирON
- Создан новый standalone проект: `КлавирON ЮKassa Backend`
  - Script ID: `1pJq9E8g2E57pB9EG2XXQvmJfFrsbjQkJNncxH8duoY1Vm2LFM0I_a1xd`
  - URL редактора: https://script.google.com/d/1pJq9E8g2E57pB9EG2XXQvmJfFrsbjQkJNncxH8duoY1Vm2LFM0I_a1xd/edit
- Код `yookassa_backend.gs` загружен через `clasp push`
- Ключи встроены в `getProp_()` как fallback (ShopID + Secret Key)
- `appsscript.json` настроен:
  - `access: ANYONE_ANONYMOUS` (доступ всем)
  - `executeAs: USER_DEPLOYING` (от имени разработчика)
  - `timeZone: Europe/Moscow`
- Web App развёрнут (deployment v2):
  - URL: `https://script.google.com/macros/s/AKfycbyYL5CnPFMfWImF_2Jv2lJAnutdrkQxRf0MTqOPPzRSndD1_tGT_2Cw-3SZWxVzte0W/exec`
- API_URL в `index.html` обновлён на новый endpoint
- Требуется одноразовая авторизация в редакторе Apps Script (Run → Review permissions → Allow)

### Что осталось:
1. ~~Авторизовать скрипт в редакторе~~ ✅ Сделано
2. ~~Проверить `?action=config` через curl~~ ✅ Работает
3. ~~Настроить webhook в ЮKassa~~ ✅ Сделано (нужно обновить URL на v3!)
4. Протестировать оплату на klaviron.ru

## 09.07.2026 — Тесты связки GAS + Google Sheets + фикс цен

### Коммит `8724be6` — API_URL обновлён + ИСТОРИЯ
- API_URL в `index.html` обновлён с v2 на новый endpoint
- История проекта обновлена
- Запушено на оба репозитория

### GAS endpoint проверен — все actions работают
- `config` → `paymentProvider: yookassa, paymentEnabled: true` ✅
- `previewResult` → тип + why + warnings из Google Sheets ✅
- `createpayment` → реальный платёж 299 ₽, URL ЮKassa ✅
- `paidResult` → модели из Google Sheets ✅ (но цены пустые!)
- `verify` → `ok: false` для фейкового order (ключи есть, DEV не активен) ✅

### Webhook в ЮKassa настроен (вручную)
- Пользователь зашёл в личный кабинет ЮKassa → Настройки → HTTP-уведомления
- URL: v2 endpoint (нужно обновить на v3!)
- События: `payment.succeeded`, `payment.canceled`

### Коммит `df2004f` — fix: findColumn_ partial match + v3 deployment

#### Проблема: пустые цены в paidResult
- GAS `findColumn_()` использовал `indexOf()` — точное совпадение
- Колонка в Google Sheets называется «Цена (руб.)», а не «цена»
- Поэтому `colPrice = -1`, и все цены возвращались пустыми

#### Фикс: findColumn_ с частичным совпадением
```javascript
function findColumn_(headers, possibleNames) {
  for (var i = 0; i < possibleNames.length; i++) {
    var needle = possibleNames[i].toLowerCase();
    var idx = headers.indexOf(needle);       // точное
    if (idx >= 0) return idx;
    for (var j = 0; j < headers.length; j++) {  // частичное
      if (headers[j] && headers[j].indexOf(needle) !== -1) return j;
    }
  }
  return -1;
}
```

#### Развёртывание v3
- GAS перепушен (`clasp push -f`)
- Создан новый deployment v3: `AKfycbxvNFBehX8KyZ-f4wTfObs4hftMLqRcS1_0MIyp2Q8EcTauWcadBGrjdAcEBQGjQDlY`
- v2 deployment (`AKfycbyYL5...`) — устарел, не содержит фикса
- HEAD deployment (`AKfycbzfF43...`) — не работает как Web App
- API_URL обновлён на v3 в `index.html` и `test-gas-api.js`

#### Результат тестов (v3):
| Сценарий | Модели | Цены | realPrice |
|----------|--------|------|-----------|
| Hobby | MPK MINI PLAY MK3, AT3 | 17500 ₽, 37250 ₽ | 25 000–44 750 ₽ |
| Learning | Performer Black, Performer White | 38000 ₽, 38000 ₽ | 43 000 ₽ |
| Production | Masterkey 25 USB, Masterkey 49 USB | 6300 ₽, 8800 ₽ | 16 800–19 300 ₽ |
| Stage | Performer Black, Performer White | 38000 ₽, 38000 ₽ | 43 000 ₽ |
| All-in-one | MPK MINI PLAY MK3, AT3 | 17500 ₽, 37250 ₽ | 25 000–44 750 ₽ |

#### Баг с бесконечным квизом исправлен
- `sessionToken = null` добавлен в `resetQuiz()`
- Одна оплата = один результат (после «Пройти заново» → снова paywall)

#### test-gas-api.js создан
- Node.js скрипт для тестирования всех GAS endpoints без оплаты
- 13 тестов: config, 5×previewResult, 5×paidResult, verify, createpayment
- 12/13 проходят (verify ожидаемо падает — ключи есть, DEV не активен)

### ⚠️ Что осталось сделать:
1. **Обновить webhook в ЮKassa** на v3 URL:
   ```
   https://script.google.com/macros/s/AKfycbxvNFBehX8KyZ-f4wTfObs4hftMLqRcS1_0MIyp2Q8EcTauWcadBGrjdAcEBQGjQDlY/exec
   ```
2. **Протестировать оплату на klaviron.ru** end-to-end
3. **Фильтрация моделей** — сейчас `filterModels_` берёт первые 2 модели без учёта budget/goal
4. **Логика фильтрации** — нужно фильтровать по колонкам «Категория», «Бюджет» в Sheets

### GAS deployments (актуальные):
| Version | Deployment ID | URL |
|---------|---------------|-----|
| HEAD | AKfycbzfF43... | Не работает как Web App |
| v1 | AKfycbzHinm... | Устарел (Tinkoff) |
| v2 | AKfycbyYL5C... | Устарел (без фикса цен) |
| **v3** | **AKfycbxvNFBehX8...** | **Актуальный** |

### Текущий GAS endpoint (v3):
```
https://script.google.com/macros/s/AKfycbxvNFBehX8KyZ-f4wTfObs4hftMLqRcS1_0MIyp2Q8EcTauWcadBGrjdAcEBQGjQDlY/exec
```

## 09.07.2026 — Критический фикс: верификация оплаты + paidResult

### Коммит `15e5c93` — fix: убран DEV-fallback в верификации + paidResult после оплаты

#### Проблема 1: Бесплатный доступ без оплаты 🔴
- Пользователь нажимает «Оплатить» → переходит на ЮMoney → нажимает «Назад» (без оплаты)
- Попадает на лендинг с баннером «Оплата подтверждена!»
- Может смотреть результат без оплаты, многократно

#### Причина
В `handlePaymentReturn()` и `startPayment()` был DEV-fallback:
```javascript
// СТАРЫЙ КОД (уязвимый):
sessionToken = data.ok ? (data.token || orderId) : 'dev_' + Date.now();
// Даже если data.ok === false, ставился sessionToken = 'dev_...'
```

#### Фикс 1: handlePaymentReturn()
- `data.ok === true` → sessionToken установлен, «✅ Оплата подтверждена!»
- `data.ok === false` → sessionToken НЕ ставится, «❌ Оплата не подтверждена» + кнопка «Повторить оплату»
- `catch (e)` → sessionToken НЕ ставится, «⚠️ Не удалось проверить оплату» + кнопка «Повторить»

#### Фикс 2: startPayment()
- Если `createpayment` упал → toast «Ошибка сети» → возврат на paywall (кнопка разблокируется)
- Если `data.ok === false` → toast «Не удалось создать платёж» → возврат на paywall
- Убран `sessionToken = 'dev_'` полностью

#### Проблема 2: Модели из локального каталога вместо Google Sheets
- После оплаты вызывался `previewResult` (без моделей)
- Фронтенд использовал локальный `QuizEngine.pickResult()` для моделей
- Модели были одни и те же при любом сценарии

#### Фикс 3: paidResult после оплаты
```javascript
// Если sessionToken есть → paidResult (модели из Google Sheets)
// Если sessionToken нет → previewResult (только тип + why)
const actionName = sessionToken ? 'paidResult' : 'previewResult';
```

Теперь после оплаты сайт запрашивает `paidResult` у GAS, который читает модели и цены из Google Sheets.

### Что осталось:
1. ~~Обновить webhook в ЮKassa на v3 URL~~ ✅ Сделано пользователем
2. **Протестировать оплату** на klaviron.ru end-to-end
3. **Улучшить фильтрацию моделей** в GAS (по budget/goal)
4. Webhook может задерживаться до 1 минуты — при тесте ждать

## 16.07.2026 — Бэкап + конфигурация перед улучшением GAS

### Текущий бэкап:
- **Коммит:** `4ed9b2c` (HEAD → main, origin/main, org/main)
- **Файлы:** `index.html`, `quiz-engine.js`, `test-gas-api.js`, `Код.js` (GAS), `appsscript.json`
- **GAS deployment:** v3 — `AKfycbxvNFBehX8KyZ-f4wTfObs4hftMLqRcS1_0MIyp2Q8EcTauWcadBGrjdAcEBQGjQDlY`
- **API_URL:** `https://script.google.com/macros/s/AKfycbxvNFBehX8KyZ-f4wTfObs4hftMLqRcS1_0MIyp2Q8EcTauWcadBGrjdAcEBQGjQDlY/exec`
- **Google Sheets:** `1fBwrXb1DU-5iMjfEeiuzWBXA85XczWLXZM3Ag_LVCVE` (лист gid=1303803798)

### Конфигурация:
- `PAYMENT_ENABLED = true`
- `ЮKassa ShopID:` 1402830
- `Secret Key:` встроен в `getProp_()` GAS
- `Webhook:` настроен на v3 URL (payment.succeeded, payment.canceled)
- `Тесты:` quiz-engine 29/29 ✅, GAS 11/13 (verify + createpayment падают)

### Карта логики квиза:
- Создан `QUIZ_LOGIC_MAP.md` — полная карта всех ~51 сценариев
- 22 уникальные ситуации, 6 типов инструментов

### GAS-тесты выявили:
- `filterModels_` не фильтрует по типу клавиатуры / категории / цене
- Модели из Sheets не соответствуют типу (MIDI вместо синтезатора и т.д.)
- `createpayment` возвращает HTML (возможно нужна повторная авторизация)

### Структура таблицы Google Sheets (колонки):
1. №
2. ID
3. Бренд
4. Модель
5. Категория
6. Тип инструмента
7. Клавиши
8. Полифония
9. Тембры
10. Ритмы
11. Чувствительность
12. Динамики
13. Страна
14. **Название (полное)** — марка + модель
15. Магазин
16. Город
17. **Цена (руб.)**
18. Наличие
19. **URL инструмента**
20. URL магазина

### План работ:
1. Исправить `filterModels_` — фильтр по типу клавиатуры, категории, цене
2. 3 модели вместо 2
3. Добавить URL + Название (полное) в ответ
4. Перепушить и переразвёрнуть GAS
5. Прогнать тесты

### Fix: заглавные буквы в quiz-engine.js
- «Компромиссное сценическое» → «Компромиссное Сценическое»
- «Профессиональное сценическое» → «Профессиональное Сценическое»
- Тесты: 29/29 ✅
- `вариант/quiz-engine.js` синхронизирован

## 09.07.2026 — План на завтра (10.07.2026)

### Приоритет 1: Тест оплаты end-to-end
- Пройти квиз → оплатить 299 ₽ → проверить redirect
- Если webhook не пришёл → проверить в логах GAS (Stackdriver)
- Если verify возвращает `ok: false` → проверить orders в Google Sheets

### Приоритет 2: Улучшить результат квиза
1. **3 модели вместо 2** — `filterModels_` в GAS сейчас берёт `slice(0, 2)`, изменить на `slice(0, 3)`
2. **Ссылки на модели** — в Google Sheets есть колонка «URL инструмента»:
   - GAS должен возвращать `url` для каждой модели
   - Фронтенд делает название модели кликабельной ссылкой
   - `readCatalogFromSheet_` — добавить чтение колонки URL
   - `getPaidResult_` — добавить `url` в объект модели
   - `renderModels` — обернуть название в `<a>` (с `target="_blank"`)

### Приоритет 3: Фильтрация моделей по бюджету/goal
- Сейчас `filterModels_` фильтрует только по accompaniment и format
- Нужно добавить фильтр по:
  - **Категория** (колонка «Категория» в Sheets) → сопоставить с goal
  - **Цена** → сопоставить с budget (xlow/low/mid/high)
- Это уберёт «одни и те же модели» для разных сценариев

### Приоритет 4: UX после оплаты
- Сохранять `sessionToken` в `localStorage` (чтобы при перезагрузке не терялся)
- Но сбрасывать при «Пройти заново»
- Или: привязать `sessionToken` к конкретным ответам квиза

## Структура проекта

```
C:\Users\user\Yandex.Disk\Документы\Код\VS cod\Cline\
├── klaviron-saas/                    # Рабочая папка проекта
│   ├── index.html                    # Лендинг + квиз
│   ├── privacy.html                  # Политика конфиденциальности
│   ├── terms.html                    # Условия использования
│   ├── README.md
│   └── ИСТОРИЯ_ПРОЕКТА.md            # Этот файл
│
└── landing-audits/                   # Папка аудитов
    ├── klaviron-saas-2026-07-04.md   # Первый аудит
    ├── klaviron-saas-2026-07-06.md   # Повторный аудит
    └── klaviron-saas-2026-07-07-fixes.md  # Отчёт об исправлениях
```

## Git remotes
- `origin` → https://github.com/zakharovvb/klaviron-saas.git (старый, backup)
- `org` → https://github.com/klaviron/klaviron.github.io.git (новый, основной)

## Google Sheets (источник данных)
- Таблица: https://docs.google.com/spreadsheets/d/1fBwrXb1DU-5iMjfEeiuzWBXA85XczWLXZM3Ag_LVCVE
- Лист: gid=1303803798
- Столбцы: Клавиши, Динамики, Тип клавиатуры — заполнены
- Ошибки для ручного исправления: CT-S100C2 (строка 55), PSR-SX600 (строка 538)

## GAS API
- Endpoint: https://script.google.com/macros/s/AKfycbyfFLVPQQXOVvhrQaXHSufbKr37WVW4fFArIVwHP3Hp_zEQRSskuIELHIHAQDLss5H1/exec
- Action: `previewResult`
- Возвращает: type, summary, why, warnings (без models/accessories/realPrice)
- Fallback: localCatalog в index.html

## Перенос на другую машину

### Что синхронизируется автоматически (через Yandex.Disk):
- Весь код проекта (`klaviron-saas/`)
- Файлы аудитов (`landing-audits/`)
- Git-репозиторий (включая `.git/` с remotes и историей)
- Файл истории проекта

### Что нужно сделать на новой машине:
1. Установить Yandex.Disk и дождаться синхронизации
2. Установить VS Code
3. Установить Cline (расширение)
4. Открыть папку `C:\Users\user\Yandex.Disk\Документы\Код\VS cod\Cline\klaviron-saas`
5. Git remotes уже в `.git/config` — пушить можно сразу

### VS Code Settings Sync (опционально):
- Включите Settings Sync в VS Code (через GitHub аккаунт)
- Синхронизирует: настройки, расширения, горячие клавиши
- На новой машине: войти в тот же GitHub → Settings Sync включится автоматически
## 16.07.2026 (вечер) — DNS error на Google Apps Script

### Проблема:
- pi.yookassa.ru — DNS error при вызове UrlFetchApp (временная)
- yoomoney.ru/api/v3/ — тоже DNS error (пробовали v9, не помогло)
- createpayment работает (платёж создаётся), но erify падает
- Это проблема инфраструктуры Google Apps Script, не кода

### GAS deployments (актуальные):
- v8 (AKfycbx6z89gr...) — актуальный, api.yookassa.ru, payment_id fix
- v9 (AKfycbymdDbHeg...) — yoomoney.ru, DNS error, не используется

### Что исправлено в v8:
1. payment_id сохраняется (saveOrder_ принимает paymentId)
2. createPaymentFromQuiz_ — создаёт платёж → сохраняет order с payment.id
3. verifyPaymentServer_ — если payment_id есть → запрашивает статус напрямую
4. Fallback в verify — если payment_id пустой → ищет по order_id через GET /payments?limit=20
5. SberPay capture — waiting_for_capture → автоматически подтверждает
6. yookassaCapturePayment_ — новая функция для подтверждения
7. yookassaFindPaymentByOrderId_ — новая функция для поиска по order_id

### Verify проверен — работает (когда DNS стабилен):
`json
{ ok:true,token:klv_dc79b6d47a914559,status:succeeded}
`

### Коммиты:
- 8a75a55 — fix: GAS v8 — payment_id fix + fallback
- 9d5b46d — docs: ИСТОРИЯ — GAS v4-v8 зафиксировано
- 6dc3b66 — fix: удалил случайный файл

### Текущий GAS endpoint (v8):
`
https://script.google.com/macros/s/AKfycbx6z89grXttEeFwqU0mMJw_9qKBoZLEDhQ_8lMgooPQRBFQBIoHlsa2RFv5bnoegByT/exec
`

### План:
1. Подождать стабилизации DNS (30-60 мин)
2. Тест оплаты на klaviron.ru
3. Если DNS error повторяется — добавить try-catch в verify
4. Если DNS error постоянный — миграция на Yandex Cloud Functions

### Альтернатива: Yandex Cloud Functions
- clasp НЕ работает с Яндекс (только Google Apps Script)
- Для Яндекс нужен yc CLI (Yandex CLI)
- Архитектура: Cloud Functions (Node.js) + Object Storage (JSON каталог)
- Сложность: средняя (код GAS → Node.js, ~2-3 часа)
- Преимущество: тот же дата-центр что и ЮKassa → нет DNS проблем

### Тесты:
- quiz-engine: 29/29 ✅
- GAS (когда DNS работает): 12/13 ✅
- Verify для реального платежа: ok:true (когда DNS работает) ✅

## 18.07.2026 — GAS v9: API URL fix + try-catch + createpayment работает

### Проблема:
- GAS v8 (AKfycbx6z89gr...) — DNS error при вызове api.yookassa.ru
- Локальный `Код.js` использовал устаревший API URL: `yoomoney.ru/api/v3/` вместо `api.yookassa.ru/v3/`
- На сервере был развёрнут код с правильным `api.yookassa.ru`, но DNS error мешал вызовам

### Фикс — GAS v9:
1. **API URL исправлен:** `yoomoney.ru/api/v3/` → `api.yookassa.ru/v3/` в `Код.js`
2. **try-catch добавлен** во все 4 функции с `UrlFetchApp.fetch`:
   - `yookassaCreatePayment_` — возвращает `{status:'error', description:'...'}` при DNS
   - `yookassaCapturePayment_` — возвращает `null` при DNS
   - `yookassaFindPaymentByOrderId_` — возвращает `null` при DNS
   - `yookassaGetPayment_` — возвращает `null` при DNS
3. **Файлы синхронизированы:** `Код.js` ↔ `yookassa_backend.gs` (v8 логика + try-catch)
4. **API_URL обновлён** в `index.html` и `test-gas-api.js` → v9 endpoint

### Deployment v9:
- **Deployment ID:** `AKfycbximr0HnMHTOULfVeYabOrSuduXZv8CwBrsHAfl6zi4w5ZoHX-WW6qRkpulXXwJvtk`
- **Endpoint:** `https://script.google.com/macros/s/AKfycbximr0HnMHTOULfVeYabOrSuduXZv8CwBrsHAfl6zi4w5ZoHX-WW6qRkpulXXwJvtk/exec`
- Создан через `clasp deploy --description "v9 - API URL fix + try-catch for DNS"`

### Результат теста:
- `config` → ✅ работает
- `verify` (фейковый order) → ✅ `{"ok":false}` (корректно)
- `createpayment` → ✅ **платёж создаётся!**
  - `orderId: klv_cedfda1032434f7c`
  - `paymentUrl: https://yoomoney.ru/checkout/payments/v2/contract?orderId=...`
- DNS error прошёл (временный, как и предполагалось)

### GAS deployments (актуальные):
| Version | Deployment ID | URL |
|---------|---------------|-----|
| v8 | AKfycbx6z89gr... | Устарел (DNS error) |
| **v9** | **AKfycbximr0Hn...** | **Актуальный** |

### Текущий GAS endpoint (v9):
```
https://script.google.com/macros/s/AKfycbximr0HnMHTOULfVeYabOrSuduXZv8CwBrsHAfl6zi4w5ZoHX-WW6qRkpulXXwJvtk/exec
```

### Что осталось:
1. Обновить webhook в ЮKassa на v9 URL
2. Тест оплаты end-to-end на klaviron.ru
3. Если DNS error вернётся — миграция на Yandex Cloud Functions

### Тесты:
- quiz-engine: 29/29 ✅
- GAS v9: createpayment работает ✅

---

## 18.07.2026 — Фиксы ₽ + retry в verify + ссылки на Яндекс Маркет

### Коммит `0a579e9` — Символ ₽ + retry в verify
- **Замена `?` на `₽`** (9 вхождений):
  - Секция «Параметры» (комплектация): `+3 000-7 000 ?` → `₽`
  - 4 кнопки бюджета: «До 20 000 ?», «20 000–50 000 ?», «50 000–90 000 ?», «От 90 000 ?» → `₽`
  - Кнопка paywall: «Получить подбор — 299 ?» → `₽` (HTML + JS fallback ×2)
  - FAQ: «+3 000-7 000 ?» → `₽`
- **Retry-логика в verify** — новая функция `verifyWithRetry(orderId, maxAttempts, delayMs)`:
  - 3 попытки с задержкой 3 секунды
  - Показывает счётчик: «Проверяем оплату… попытка N из 3»
  - Обрабатывает ошибки сети и неудачные ответы
  - `handlePaymentReturn` переписан для использования этой функции
  - Удалён старый try/catch блок (ошибки теперь внутри verifyWithRetry)

### Коммит `e193539` — Ссылки на Яндекс Маркет (столбец Y)
- `readCatalogFromSheet_()` — изменён поиск столбца URL:
  - Было: `findColumn_(headers, ['url инструмента', 'url', 'ссылка'])`
  - Стало: `findColumn_(headers, ['ссылка поиска на яндекс маркет', 'яндекс маркет', 'url инструмента', 'url', 'ссылка'])`
- Обновлены оба файла: `Код.js` (Desktop) и `yookassa_backend.gs` (git)
- **GAS deployment @17** — production URL не изменился (`AKfycby1XNc...`)

### Проверка Google Sheets (18.07.2026):
- `config` → ✅ `{"ok":true,"paymentEnabled":true,"price":29900,"priceLabel":"299 ₽"}`
- `previewResult` → ✅ тип + summary + why из таблицы
- `paidResult` → ✅ 3 модели с ценами и ссылками на Яндекс Маркет:
  ```
  1. CASIO AT3 — 37 250 ₽ — https://market.yandex.ru/search?text=CASIO%20AT3
  2. CASIO AT5 — 42 380 ₽ — https://market.yandex.ru/search?text=CASIO%20AT5
  3. CASIO CT-X3000 — 40 000 ₽ — https://market.yandex.ru/search?text=CASIO%20CT-X3000
  ```

### GAS deployments (актуальные):
| Version | Deployment ID | Status |
|---------|---------------|--------|
| v9 | AKfycbximr0Hn... | Устарел |
| v10 | AKfycby1XNc... | **Актуальный (@17)** |

### Текущий GAS endpoint (v10):
```
https://script.google.com/macros/s/AKfycby1XNcboUcBoI_4g7DDmwtGEpfMMGMKsBrJ2irceuOTJv6PLQQriXmZFmd5x_EADQo/exec
```

---

## МЕХАНИКА ПРОЦЕССА ОПЛАТЫ (end-to-end)

### Поэтапное описание:

**1. Пользователь проходит квиз**
- Отвечает на вопросы (goal, experience, format, budget и т.д.)
- На последнем шаге вызывается `submitQuiz()`

**2. Показ paywall**
- `PAYMENT_ENABLED = true` и `sessionToken = null` → показывается paywall
- `pickLocalResult()` определяет тип инструмента (из QuizEngine) и показывает его бесплатно
- Кнопка: «Получить подбор — 299 ₽»

**3. Нажатие кнопки «Получить подбор»**
- Вызывается `startPayment(payload)`
- Кнопка меняет текст на «Перенаправляем на оплату…»
- Фронтенд шлёт GET-запрос к GAS:
  `?action=createpayment&goal=hobby&budget=low&experience=beginner&format=synth&amount=29900`

**4. GAS создаёт платёж в ЮKassa**
- `createPaymentFromQuiz_()` генерирует `orderId = klv_XXXXXXXX...`
- Вызывает `yookassaCreatePayment_()` → POST на `https://api.yookassa.ru/v3/payments`
- ЮKassa возвращает `confirmation_url`
- GAS сохраняет заказ в Google Sheets (лист `orders`): order_id, status=NEW, goal, budget, amount
- Возвращает фронтенду: `{ ok: true, orderId, paymentUrl }`

**5. Редирект на ЮKassa**
- `window.location.href = data.paymentUrl` — пользователь попадает на страницу ЮKassa
- Пользователь вводит данные карты / выбирает СБП / SberPay
- Оплачивает 299 ₽

**6. ЮKassa обрабатывает платёж**
- При успехе ЮKassa шлёт **webhook** (POST) на GAS: `payment.succeeded`
- GAS обновляет статус в Sheets: `succeeded`
- Если SberPay (`waiting_for_capture`) → GAS автоматически подтверждает через `yookassaCapturePayment_()`
- ЮKassa редиректит пользователя обратно: `https://klaviron.ru/?payment=ok&order_id=klv_XXX`

**7. Возврат на сайт — проверка оплаты**
- `handlePaymentReturn()` срабатывает при загрузке
- Видит `?payment=ok&order_id=klv_XXX`
- Очищает URL (`replaceState`)
- Показывает спиннер «Проверяем оплату…»

**8. Verify с retry (новая логика)**
- Вызывается `verifyWithRetry(orderId, 3, 3000)`:
  - **Попытка 1**: GET `?action=verify&order_id=klv_XXX`
    - GAS проверяет в Sheets статус заказа
    - Если `succeeded` → возвращает `{ ok: true, token: orderId }`
    - Если нет → проверяет напрямую через ЮKassa API (`yookassaGetPayment_`)
    - Если платёж найден и подтверждён → обновляет Sheets, возвращает ok
  - Если ответ не ok или ошибка сети → ждёт 3 секунды, показывает «попытка 1 из 3»
  - **Попытка 2**: то же самое
  - **Попытка 3**: последняя попытка
  - Если все 3 неудачны → возвращает `null`

**9. Если оплата подтверждена (`data.ok === true`)**
- `sessionToken = data.token` (или orderId)
- Показывает экран «✓ Оплата подтверждена! Полный отчёт разблокирован»
- Кнопка «Смотреть результат»
- Нажатие → `submitQuiz()` снова, но теперь с `sessionToken`

**10. Загрузка платного результата**
- `submitQuiz()` шлёт `?action=paidResult` (вместо `previewResult`)
- GAS вызывает `getPaidResult_()`:
  - Читает каталог из Google Sheets (`readCatalogFromSheet_`)
  - Фильтрует модели по параметрам (бюджет, тип, формат)
  - Возвращает 3 модели с ценами и ссылками на **Яндекс Маркет**
  - Плюс комплектация, реальная цена, предупреждения
- Фронтенд рендерит полный результат

**11. Если оплата НЕ подтверждена**
- После 3 попыток → экран «Оплата не подтверждена после 3 попыток»
- Кнопка «Повторить оплату» → возврат к началу

### Возможные проблемы:
- **Webhook не пришёл** — retry через 3 сек + прямой запрос к ЮKassa API ловят платёж
- **Webhook не настроен** — `verifyPaymentServer_` имеет fallback: ищет платёж по order_id в metadata
- **DNS error в GAS** — try-catch во всех функциях с UrlFetchApp.fetch


## 29.07.2026 — Фикс: sessionStorage->localStorage — format=hammer терялся после оплаты

### Проблема:
- Пользователь выбирал молоточковую -> после оплаты получал Обучающий синтезатор
- sessionStorage очищается при переходе на домен ЮKassa и обратно

### Фикс:
1. sessionStorage -> localStorage — сохраняется между доменами
2. Добавлены console.log для диагностики

### Тест (29.07.2026, learning+intermediate+hammer+mid):
- Тип: Цифровое пианино
- Модели (3 разных бренда): CASIO CDP-S160, Kurzweil KA70, Yamaha YDP-103 (по 50000)
- format=hammer дошел до GAS -> правильный результат

### Коммиты:
- bbc72b5 — fix: hobby+hammer=Цифровое пианино any budget + blacklist (GAS v27)
- 0d2ffa9 — fix: sessionStorage->localStorage

### GAS deployment: v27
- AKfycby0KNt5a418e2Ikx3U5MjVuT5uYVUjNnUizIulK2BLhacEKPSjLePZEbTRN2PZ_cqKH @37

---

## 19.07.2026 — GAS v26: filterModels_ — разные бренды + priceNum=0

### Коммит — feat: filterModels_ разные бренды + priceNum=0 + brand в каталог

#### Что сделано:
1. **`readCatalogFromSheet_`** — добавлено чтение колонки «Бренд» (`colBrand`)
2. **`filterModels_`** — два фикса:
   - **priceNum=0 исключение**: модели без цены больше не попадают в результат
   - **Разные бренды**: сортировка по цене + выбор по одной модели от каждого бренда
3. **API_URL исправлен** в `index.html` и `test-gas-api.js` (был пустой ID)
4. **GAS deployment v26** — `AKfycbwtcSwycMUsDrkV47JS5rcrMwXmKKI3wJoObOJ4o_s-lIOedY-L-MZbjFLV61pCp6k @36`

#### Результат тестов (12/13, verify ожидаемо падает):
- Hobby: CTS200RD, PSR-E263, MK401 — 3 разных бренда ✓
- Learning: DDP-80, Elegy Black, UP82 BK — 3 разных бренда ✓
- Production: KROSS, PRO MPK249, XPS-10 — 3 разных бренда ✓
- Stage: DP460K, DGX-650B, C1 AIR-BK — 3 разных бренда ✓
- All-in-one: NP-V80, CT-S1100V, i3 — 3 разных бренда ✓

#### Тесты:
- quiz-engine: 29/29 ✅
- GAS: 12/13 ✅ (verify ожидаемо падает — ключи есть, DEV не активен)

---

## 18.07.2026 — GAS v11: критический фикс normalizeApiResult + hobby+hammer type

### Проблема (болевой тест):
После оплаты пользователь видел:
- **2 модели вместо 3**
- **Без ссылок на Яндекс Маркет**
- **Несоответствие по типу клавиатуры** (hobby+hammer показывал «Обучающий синтезатор» вместо «Цифровое пианино»)

### Корневая причина №1: normalizeApiResult() (КРИТИЧЕСКИЙ БАГ)
- GAS `getPaidResult_` возвращает данные в обёртке: `{"ok":true,"data":{...}}`
- Фронтенд `normalizeApiResult()` искал данные в `apiData.result` (которого **не существует**)
- Из-за этого `root = apiData` (весь объект с обёрткой), `root.type = undefined`
- Функция возвращала `null` → **срабатывал локальный fallback** из `quiz-engine.js`
- Локальный fallback содержит только 2 модели без ссылок

### Фикс:
```javascript
// БЫЛО (баг):
const root = apiData.result && typeof apiData.result === 'object' ? apiData.result : apiData;

// СТАЛО (фикс):
let root = apiData;
if (apiData.data && typeof apiData.data === 'object') root = apiData.data;
else if (apiData.result && typeof apiData.result === 'object') root = apiData.result;
```

### Корневая причина №2: determineTypeGAS_ не учитывал hobby+hammer
- `quiz-engine.js`: `hobby + hammer + (mid/high)` = «Цифровое пианино»
- `determineTypeGAS_`: всегда возвращал «Обучающий синтезатор» для hobby
- **Несоответствие** между локальной логикой и GAS-бэкендом

### Фикс:
```javascript
// Добавлено в determineTypeGAS_:
if (goal === 'hobby' && format === 'hammer' && (budget === 'mid' || budget === 'high')) {
  return {
    type: 'Цифровое пианино',
    summary: 'Молоточковая механика для домашних занятий.',
    why: ['Молоточковая механика для фортепианной техники.', '88 клавиш — полный диапазон.', 'Подходит для домашних занятий.']
  };
}
```

### GAS v11 deployment:
- **Deployment ID:** `AKfycbyiTs8OLDT0_AKad-uaY4ocKWHL9BxVB_XUz5krzawsFqOmTAe96loPoQSFAeSLTHFb`
- **Endpoint:** `https://script.google.com/macros/s/AKfycbyiTs8OLDT0_AKad-uaY4ocKWHL9BxVB_XUz5krzawsFqOmTAe96loPoQSFAeSLTHFb/exec`
- API_URL обновлён в `index.html` и `test-gas-api.js`

### Результат тестов (11/13 прошли):
| Сценарий | Тип | Модели | realPrice |
|----------|-----|--------|-----------|
| Hobby + synth + low | Обучающий синтезатор | 3 (AT3, AT5, CT-X3000) | 44 750–49 880 ₽ |
| Learning + hammer + low | Цифровое пианино | 3 (Performer B/W, CDP-S110) | 43 000–47 000 ₽ |
| Production + MIDI + mid | Рабочая станция | 3 (PRO MPK, KROSS) | 60 500–76 500 ₽ |
| Stage + hammer + high | Сценическое пианино | 3 (KRONOS2-88) | 191 250–373 000 ₽ |
| All-in-one + synth + mid | Универсальный инструмент | 3 (CTK-7200, CTS1000V, CTX3000) | 61 900–63 900 ₽ |

### Что осталось:
1. **Обновить webhook в ЮKassa** на v11 URL
2. **Тест оплаты end-to-end** на klaviron.ru
3. Если DNS error вернётся — миграция на Yandex Cloud Functions

